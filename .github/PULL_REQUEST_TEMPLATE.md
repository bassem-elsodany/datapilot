## Summary

What does this PR change, and why?

## Test plan

- [ ] Backend imports cleanly (`uv run python -c "import app.main"`) — if backend touched
- [ ] Dashboard builds (`npm run build`) — if dashboard touched
- [ ] Manually verified the affected feature in the running app
